specVersion: "v1"
serviceId: "beta"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "beta-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
